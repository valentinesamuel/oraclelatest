version: '3.8'
services:
  oracle-worker:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3001"
    mem_limit: 512m
    cpus: "1.0"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
